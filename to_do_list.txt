- Làm staging cho all bảng

- bảng transaction rất phù hợp với model incremental vì nó:
    - dữ liệu 1 khi đã chèn vào là sẽ không thay đổi (tính toàn vẹn dữ liệu)
    - thường xuyên có record mới với số lượng lớn
    (bảng loan_repayments cũng có thể dùng model incremental)
Tuy nhiên với staging thì cứ dùng view để dữ liệu giống trong raw


thêm test cho cả 12 bảng:
branch ok
customer
